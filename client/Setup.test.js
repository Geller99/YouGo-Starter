const { render } = require("jest")


// run npm jest for test suites
// $ npm run test -- --watch to watch for changes


describe('Test for App js', () => {
    
    it('should render when clicked', () => {
        
    })
})